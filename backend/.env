# Backend environment variables
# Set to "true" to allow all origins (useful for development)
ALLOW_ALL_ORIGINS=true

# Port for the backend server
PORT=8000

# Host for the backend server (0.0.0.0 allows external connections)
HOST=0.0.0.0
